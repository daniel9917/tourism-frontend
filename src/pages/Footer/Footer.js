import { Grid, Typography } from "@mui/material";

const Footer = () => {
    return (
        <Grid container justifyContent={"space-evenly"} maxWidth={1} maxHeight={0.25} bgcolor = '#aa3'>
            <Typography variant="h3">Footer</Typography>
        </Grid>
    );

}

export default Footer;